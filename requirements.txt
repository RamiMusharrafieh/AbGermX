numpy>=2.0
pandas>=2.0
scipy>=1.10
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4

# Optional, not required by any script here but a natural drop-in swap for the
# t-SNE calls in run_dimred.py / run_dimred_cdr_only.py / compare_query_to_species.py
# if you want to compare against UMAP:
# umap-learn>=0.5
