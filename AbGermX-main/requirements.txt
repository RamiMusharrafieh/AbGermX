numpy>=2.0
pandas>=2.0
scipy>=1.10
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4

# Optional extras, not imported by anything here:
#   fastcluster  - speeds up the clustermap linkage on large identity matrices
#   umap-learn   - a drop-in swap for the TSNE calls in run_dimred.py,
#                  run_dimred_cdr_only.py and compare_query_to_species.py
# fastcluster>=1.2
# umap-learn>=0.5
